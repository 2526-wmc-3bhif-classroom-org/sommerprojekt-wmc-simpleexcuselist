# Login & Parent Creation
Currently, when a student logs into the system for the first time using their WebUntis credentials, a corresponding Parent account is automatically generated in the background.
**For testing purposes:** The generated parent username (which starts with gu...) and the plain text password for this account are printed directly to the server console. You can use these credentials from the terminal output to test the parent login flow.
